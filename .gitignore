# Environment variables — NEVER commit these
.env
.env.local
.env.production

# NOTE: assets/js/config.js is intentionally committed.
# It contains the Supabase ANON public key (safe to expose by design —
# RLS policies are the security boundary) and the Supabase project URL.
# NEVER put the Supabase service role key or Anthropic API key here.

# OS files
.DS_Store
Thumbs.db

# Editor
.vscode/settings.json
.idea/

# Logs
*.log

# Node (if added later)
node_modules/
